# Workbench Scope

This resource reports the current state for this Workbench MCP session. It is
for durable scope that should travel with the current interaction: focus,
artifact id, scoped artifact resource, and local scoped Markdown path.

Discover active tools, resources, resource templates, and prompts through MCP
list endpoints. This document intentionally does not duplicate capability
listings.
