// Package resources defines Workbench MCP resource descriptors and embedded
// static resource content.
//
// The package owns resource identity, URI conventions, and static Markdown.
// Runtime read handlers remain in package mcp because they depend on live
// context and artifact stores.
package resources
